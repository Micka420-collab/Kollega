# Backlog — ce qu'on refuse de coder maintenant

Une fonctionnalité s'écrit quand trois clients payants l'ont demandée
(constitution v2). Avant ça, elle vit ici.

| Date | Idée | Origine | Compteur de demandes client payantes |
|---|---|---|---|
| 2026-07-26 | « Plateforme innovante / architecture pour se démarquer » — reformulé : toute généralisation au-delà du runtime gouverné V0 | Conversation de démarrage | 0 |
| ~~2026-07-26~~ | ~~Durcir le test du graphe de dépendances : vérifier le graphe résolu plutôt que le TOML brut~~ — **FAIT le 29/07** : la fermeture transitive est lue dans `Cargo.lock` (pas besoin d'invoquer `cargo metadata` depuis un test). Vérifié par sabotage : ajouter `tokio` au domaine fait apparaître **tokio, socket2 ET mio** — les deux derniers par transitivité, invisibles pour l'ancienne garde. | Revue M0 | fait |
| 2026-07-29 | **L'organisation vient du CHEMIN dans les routes de tâches** (`/orgs/{org}/tasks/{task}`), faute de session — l'authentification est M1. La RLS empêche un client de LIRE les données d'un autre, mais pas d'ÉCRIRE chez lui : le contexte qu'il choisit devient le sien. Échafaudage à remplacer par l'organisation issue de la session AVANT toute mise en ligne. | Étape 2 de la tranche du 29/07 — interdit de corriger cette nuit (M1 hors périmètre) | s/o (dette de sécurité, pas une demande client) |
| 2026-07-28 | Remplacer le modèle M4 « comptes rendus » par « relance client et suivi des impayés » (et re-prioriser les trois modèles) | `docs/taches-delegables-analyse.md` §6 — recommandation du propriétaire, décision produit à trancher par lui | s/o (décision produit, pas une demande client) |
| 2026-07-28 | Schéma M4 : le palier de confiance vit sur le couple (mandat, catégorie d'action), pas sur l'agent — la table des mandats porte un palier PAR catégorie | `docs/methode-de-travail.md` (bloc 7, correction 1) — décision de schéma gratuite aujourd'hui, coûteuse après la première migration de mandats | s/o (dette de conception) |
| 2026-07-27 | **Rien ne fait avancer une tâche toute seule** : un pas = une requête HTTP. Sans ordonnanceur (file `SKIP LOCKED`), une tâche suspendue reste suspendue jusqu'à ce qu'un client repousse un pas — l'agent ne « travaille » pas, il est poussé. | Étape 5 — hors périmètre de la nuit | s/o (dette de conception) |
| 2026-07-27 | Le message de la garde `in_ci_no_integration_test_may_skip` annonce « sept tests exigeant une base réelle » : ils sont huit depuis `tranche_over_http`. Le compte est indicatif, la garde fonctionne — mais un message faux use la confiance qu'on lui accorde. | Étape 5 — interdit de modifier une garde cette nuit | s/o (dette de documentation) |
| 2026-07-27 | La route de pas répond `503` tant qu'aucun `ModelProvider` n'est branché, et le binaire n'en branche aucun : **`kollega serve` ne peut faire avancer aucune tâche aujourd'hui**. Le choix du fournisseur réel appartient au propriétaire (`docs/questions-nuit.md`). | Étape 5 — décision d'architecture, non tranchée seul | s/o (décision propriétaire) |
| 2026-07-27 | Le déclencheur CI `push: worktree-**` a été ajouté pour qu'une branche de travail puisse être prouvée sans `gh` : à retirer le jour où les branches passeront par une pull request. | Nuit du 27/07 — outillage réversible | s/o (outillage) |
